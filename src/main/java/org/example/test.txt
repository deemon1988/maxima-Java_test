public static int multipleWeight (int a) {
        //int w =0;
        if( Math.round(a)%500 == 0) {
           // w = Math.round(a);
        }
        else {
            while (a%500!=0) {
                a -=1;
                if (a%500==0) {
                    //w = a;
                }
            }
        }
        return a;
    }




    int weight = 1701;
            int w =0;
                   if( Math.round(weight)%500 == 0) {
                       w = Math.round(weight);
                   }
                   else {
                       while (weight%500!=0) {
                           weight -=1;
                           if (weight%500==0) {
                               w = weight;
                               System.out.println(w);
                           }
                       }

                   }




                    Transport getTransport(City city, int weight, int hours) {
                           final String SHIP = "Новый Корабль";
                           final String TRUCK = "Новый Грузовик";
                           final String PLANE = "Новый Самолёт";
                           final float SHIP_PRICE = 30.05f;
                           final float TRUCK_PRICE = 10.03f;
                           final float PLANE_PRICE = 50.01f;
                           Transport transport = null;
                           int speed;

                           speed = city.getDistance() / hours;
                           System.out.println("скорость "+speed );  // скорость транспорта
                           if (speed < 40 && city.isOnWater()) transport = new Ship(SHIP, multipleWeight(weight), 30, SHIP_PRICE);
                           if (speed > 120 && city.isHasAirport()) transport = new Plane(PLANE, multipleWeight(weight), 850, PLANE_PRICE);
                           if (speed > 40 && speed <120) transport = new Truck(TRUCK, multipleWeight(weight),110, PLANE_PRICE);


                           System.out.println(transport.getName()+" - грузоподъёмность "+transport.getCapacity());
                           return transport;
                           }